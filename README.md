# GC Assistant

A userscript to enhance the geocaching.com experience.

## 目前实现了的功能

-   浏览 cache 页面时，自动显示 log 中的图片缩略图，鼠标悬停可查看大图

## Build

```
npx rollup -c
```

This will create the files in the `dist` folder.
